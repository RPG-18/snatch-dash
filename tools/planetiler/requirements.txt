# pip install -r requirements.txt
#
# Намеренно минимально: Overpass-запросы идут через urllib (стандартная
# библиотека), а не requests, чтобы не тащить лишнюю внешнюю зависимость.
PyYAML>=6.0

# Буфер вокруг границы субъекта перед нарезкой пака (cut_packs.py). Собственная
# реализация буферизации полигона с дырами — это заново написать кусок GEOS,
# поэтому здесь зависимость оправдана, в отличие от requests.
shapely>=2.0
