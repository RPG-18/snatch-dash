/// Tripper Dash frame size — mirrors the native plugin's `DashEncoder.WIDTH`/
/// `HEIGHT` (`packages/opendash_dash_engine/.../dash/video/DashEncoder.kt`).
const dashFrameWidth = 526;
const dashFrameHeight = 300;
